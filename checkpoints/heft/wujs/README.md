# HEFT WUJS

Source family: `https://github.com/Axellwppr/motion_tracking`.

The runtime contract is `policy.yaml`; the adjacent `policy.onnx` is restored
by the `reference` profile in `artifacts/lock.v1.json`.
